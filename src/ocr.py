# src/ocr.py
# Handles the Optical Character Recognition (OCR) process via Tesseract 5.

# TODO: Implement run_ocr() function that takes an image file path,
# calls the Tesseract command-line tool, and saves the output as an ALTO XML file.
# Ensure Page Segmentation Mode (PSM) is configurable.
